---
interface Props {
  id?: string;
  title: string;
  subtitle?: string;
  align?: 'center' | 'left';
}

const { id, title, subtitle, align = 'center' } = Astro.props;
---

<div class:list={['mb-10 fade-up', align === 'center' && 'text-center']}>
  <h2 id={id} class="text-3xl sm:text-4xl font-bold text-green-900 mb-4">
    {title}
  </h2>
  {subtitle && (
    <p class:list={['text-gray-600 text-lg', align === 'center' && 'max-w-2xl mx-auto']}>
      {subtitle}
    </p>
  )}
</div>
