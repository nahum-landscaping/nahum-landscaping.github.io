---
import { siteConfig } from '../../config';

interface Props {
  variant?: 'outline' | 'ghost';
  size?: 'md' | 'lg';
  class?: string;
}

const { variant = 'outline', size = 'lg', class: className = '' } = Astro.props;

const base = 'inline-flex items-center justify-center gap-2 font-semibold rounded-lg text-center transition-all duration-200 ease-out';

const variants: Record<string, string> = {
  outline: 'border-2 border-white text-white hover:bg-white/10 active:scale-[0.98]',
  ghost: 'text-green-700 hover:text-green-600',
};

const sizes: Record<string, string> = {
  md: 'px-6 py-3 text-sm',
  lg: 'px-8 py-4 text-base',
};
---

<a href={siteConfig.phoneHref} aria-label={`Call Nahum J Landscaping at ${siteConfig.phone}`} class:list={[base, variants[variant], sizes[size], className]}>
  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
    <path stroke-linecap="round" stroke-linejoin="round" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
  </svg>
  Call {siteConfig.phone}
</a>
